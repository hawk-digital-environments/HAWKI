# HTTP API

HAWKI's primary REST API surface and the request/response contracts around it.

| Page | Covers |
|---|---|
| [JSON:API Conventions](./100-JSON-API-Conventions.md) | The JSON:API v1 server at `/api/hawki/v1` — conventions, the `no_scope` parameter, dual auth. |
| [Connection Bootstrap](./200-Connection-Bootstrap.md) | The `/connections/hawki` call the SPA makes on boot; `ConnectionType`; `UserContext`/`UsageContext`. |
