<!--
  @component Default 404 fallback rendered by `RouterView` when no route
  matches the current path. Pass a custom component via `RouterView`'s
  `notFoundComponent` prop to replace it.
-->
<script lang="ts">
    interface Props {
    }

    const {}: Props = $props();
</script>
Route not found - 404 🥹
