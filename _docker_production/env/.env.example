# =====================================================
# HAWKI - Environment Configuration Template
# =====================================================
# Copy this file and customize for your deployment:
#   cp .env.example .env
#
# Or use the initialization script:
#   ./env-init.sh --profile=dev|staging|prod
#
# Most configuration values have sensible defaults in
# .env.dev, .env.staging, or .env.prod files.
# You only need to set the values below.
# =====================================================

# -----------------------------------------------------
# Application URL (REQUIRED)
# -----------------------------------------------------
# The URL where your HAWKI instance will be accessible
# Dev example: https://app.hawki.dev
# Prod example: https://hawki.your-university.edu
APP_URL=https://your-domain.com
APP_TIMEZONE="CET"
APP_LOCALE="de_DE"
# -----------------------------------------------------
# Database Password (REQUIRED)
# -----------------------------------------------------
# Secure password for MySQL database
# Will be auto-generated if you use env-init.sh
DB_PASSWORD=changeme

# -----------------------------------------------------
# Redis Password (REQUIRED)
# -----------------------------------------------------
# Secure password for Redis cache
# Will be auto-generated if you use env-init.sh
REDIS_PASSWORD=changeme

# -----------------------------------------------------
# File Converter API Key (REQUIRED)
# -----------------------------------------------------
# Secure API key for the file converter service
HAWKI_FILE_CONVERTER_API_KEY=changeme

# -----------------------------------------------------
# Encryption Keys (REQUIRED - Auto-generated)
# -----------------------------------------------------
# These keys will be automatically generated by env-init.sh
# DO NOT share these keys or commit them to version control!
#
# Generate manually with:
#   php artisan key:generate
#   echo "base64:$(openssl rand -base64 32)"
#
APP_KEY=
USERDATA_ENCRYPTION_SALT=
INVITATION_SALT=
AI_CRYPTO_SALT=
PASSKEY_SALT=
BACKUP_SALT=

# -----------------------------------------------------
# Optional: Proxy Configuration
# -----------------------------------------------------
# Only needed if your network requires a proxy
# DOCKER_HTTP_PROXY=http://proxy.example.com:3128
# DOCKER_HTTPS_PROXY=http://proxy.example.com:3128
# DOCKER_NO_PROXY=localhost,127.0.0.1

# -----------------------------------------------------
# All other configuration values are loaded from:
# - .env.dev (development mode)
# - .env.staging (staging mode)
# - .env.prod (production mode)
#
# See those files for available configuration options.
# =====================================================
